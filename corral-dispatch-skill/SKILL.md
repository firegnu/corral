---
name: corral-dispatch
description: "当主控：把开发任务拆开、写成任务文件，用 corral 开 Claude Code、Codex 等 agent 分头去做，再审查、合并、收尾。Use when the user asks you to split development work and hand it to other agents with task files, e.g. 拆任务派给别的 agent、写任务文件交给 Codex 做、开几个 agent 并行开发、按里程碑分派、交叉审查后合并, dispatch tasks to agents via corral, orchestrate several coding agents. 只是开一个 agent 问个问题、看一下东西，用 corral 技能。你自己是被委派的 agent（任务文件里写明了身份）时不要用。"
---

# corral-dispatch：当主控，按任务文件分派、审查、收尾

开 agent、送话、等待、取回复、关掉，一律按 corral 技能做。本技能只管：怎么拆、怎么交代、交给谁用哪一档、怎么审、怎么收尾。

## 先确认三件事

1. **你是主控。** 你如果是被委派的 agent（任务文件里写明了你的身份），不要用本技能：照任务文件做，也不要再开别的 agent。
2. **corral 能用。** 按 corral 技能的自检做，你自己也必须是用 corral 开的。
3. **读项目的 AGENTS.md**（或 CLAUDE.md），找下面这些项目规矩。和本技能冲突时，以项目规矩为准；没写的问用户一次，并参照本技能同目录的 `项目AGENTS模板.md`，建议补进 AGENTS.md：
   - agent 名字前缀；任务文件放哪；按什么拆（里程碑、分支、worktree 放哪）
   - 怎么合并（本地合并还是开 PR）、推不推送
   - 私有数据在哪、哪些不能读、哪些操作花钱

   交给哪家、几档、要不要交叉审查，一般不写在 AGENTS.md 里，按第 3 节定，不用问用户。项目确实写了（包括「不用路由」），照它的。

## 1. 拆任务

- 架构级的决定（数据模型、模块边界、里程碑怎么划）不要自己拍板：已经有方案或开发计划的，照它拆；没有的，先问用户要不要先定架构，等他给了结论再拆。
- 一件事、一个 agent、一个分支、一个 git worktree。一个 agent 一次只做一件事。
- 地基（数据模型、公共约定）先串行做完，再按需并行，不要一开始就开很多个。
- 并行之前：
  - 把接口约定写进任务文件（字段名照写），两边照同一份约定做；
  - 查文件范围：两个任务要改同一批文件，就不并行，改成先后；
  - 指定谁负责集成（例如后端合并后，由前端 agent 接真实接口验收）。
- 拿不准的设计问题，可以分别问一个 Claude Code 和一个 Codex，把分歧列给用户，不要自己挑一个就开工。

## 2. 写任务文件

开出来的 agent 看不到你和用户的对话。每次委派都先写任务文件，让对方先读项目说明，再读任务文件。模板：

```markdown
# 任务：<一句话>

<日期>，<你的名字> 交给 <对方名字>（<哪家 agent>，<档：模型 / 强度>）。
路由：<见第 3 节的记录格式>
你是被委派的 agent：照本文件做，不要再开别的 agent。

## 先读
1. AGENTS.md
2. <背景、设计文档里和这件事有关的节>
3. <相关的现有代码和测试>
4. 本文件

## 你在哪里干活
- worktree：<路径>，分支 <分支>（已从 main 建好）。只在这里改，只动 <目录>。
- 新 worktree 先装依赖：<命令>。
- 同时在做的还有 <谁，在哪个分支，动哪些目录>；你不要动 <目录>。

## 要做的
<分节写清楚；和别的任务之间的接口约定照写字段名>

## 验收（先写测试，确认因为功能没实现而失败，再实现）
- <一条条可检查的行为>
- 失败必须是因为目标行为还没实现；导入报错、测试数据坏了不算。
- <要实测的：怎么跑、用什么合成数据；不花钱、不碰真实数据>

## 不要做
- <范围外的事>
- 不要按项目名或路径批量杀进程（`pkill -f <项目名>` 这类）：主控和别的 agent 的进程命令行里都带着项目名和工作目录，一条命令能把它们全杀掉。停自己起的服务用起的时候记下的 PID，或者固定端口后 `lsof -ti:<端口>`。
- 拿主意的地方写进 <文档>「实现时的取舍」，并在回复里列出。
- 遇到 <某种情况>，停下来报告，等决定，不要自己换别的办法。
- 不合并到 main，不推送。只在 <分支> 上提交。

## 记录要求
做完在本文件末尾追加「## 完成记录」（在你的分支里提交）：做了什么、测试命令和结果、遇到的问题、没做的事。

## 回复
回复里只写：做完了哪些、测试结果、取舍各一句话、有没有要主控决定的事。命令都在前台跑完，全部做完后，回复最后一行写 DONE。
```

## 3. 交给哪家、几档、要不要交叉审查

每件活都要定这三样。交给哪家由你照下面的分工表定，不问路由；几档和要不要交叉审查先问路由，路由拿不准再自己按下面的规则定。项目 AGENTS.md 写了的，以及下面「很费 token」的配置，先于这些规则。

**问路由**：写一段任务摘要，三五句，中文即可：做什么、改哪些目录、有什么风险，不写私人数据。运行本技能目录下的 route.py（和这份 SKILL.md 在同一个目录）：

```sh
echo "<摘要>" | python3 <本技能目录>/route.py
```

- 输出里每一项的 `verdict` 就是结论，照用：`tier.verdict`（轻 / 常规 / 重）、`cross_review.verdict`（要 / 不要）。
- 某一项是 `null`（拿不准），或者输出 `"ok": false`（没有 key、网络不通等），这一项自己按下面的规则定，不要重试、不要问用户。项目写了「不用路由」时不调用。
- 只有在知道路由看不到的情况时（比如这件活返工过、审查发现过问题），才推翻它的结论，写一句理由。
- 在任务文件开头记一行：`路由：<档> / 交叉审查<要或不要>（路由：<各项结论，null 写拿不准，不可用写原因>；推翻：<理由，没有就不写>）`。

**交给哪家**：

| 家 | 负责 |
|---|---|
| Codex | 产品的后端：服务端、数据库、接口、纯业务规则。两家都做得了的活也给它：迁移、批量机械改动、补测试、跑很久的活。交叉审查。前端做完的浏览器验收 |
| Claude Code | 产品的前端：网页和界面。要快速来回试的活 |
| pi、omp | 项目代码之外的杂活：本机清理、一次性脚本、核对数据、跑命令汇报。两家同类，任选，一家手上有活就开另一家。只接轻档；项目代码里的改动再小也不给它 |

- 前后端都沾的，能拆就拆开；拆不开的，按主要改动的那一边。
- 拿不准哪家做得了，给 Codex：别用 Claude Code 的额度做 Codex 也能做的事。这条按订阅定——现在 Codex 额度宽松、Claude Code 有周限额；订阅变了改这条。
- 同一家已经开着写代码的 agent、手上没活、档位也和这件活一样时，优先交给它，不要让相似的活在两家之间来回换。档位不同就另开一个：开着的 agent 换不了模型和强度。

**几档**：不带参数时，开出来的 agent 用它自己的全局默认，**不继承你的模型和强度**。每次开 agent 都显式带上参数，并在任务文件和汇报里写明用的哪一档。

| 档 | 什么活 | Claude Code | Codex |
|---|---|---|---|
| 轻 | 查资料、机械改动、按清单改文字、跑命令汇报 | `--model sonnet --effort medium` | `-m gpt-5.6-luna -c 'model_reasoning_effort="medium"'` |
| 常规（默认） | 按任务文件写功能、修一般的 bug | `--model 'opus[1m]' --effort high` | `-m gpt-6-astra -c 'model_reasoning_effort="high"'` |
| 重 | 数据模型、并发和事务、跨模块设计、难查的 bug、交叉审查 | `--model 'opus[1m]' --effort xhigh` | `-m gpt-6-astra -c 'model_reasoning_effort="xhigh"'` |

- 拿不准用常规；审查发现质量不够，再升一档重做。
- 下面这些很费 token，可以提议，但要先问用户，同意了才开：Claude Code 用 `fable`（任何强度）、opus 开 `max`；Codex 开 `ultra`（它还会自己再开子 agent）。用户点名就算同意。
- pi、omp 用它们的默认，不分档。
- 模型名会随版本变，过时了改这张表。

**要不要交叉审查**：见第 6 节。

## 4. 派出去

按 corral 技能开 agent，另外注意：

- 名字用 AGENTS.md 规定的前缀，`--cwd` 指向它的 worktree，agent 命令后面带上这一档的参数。第一句话很短，只让它去读文件：

  ```sh
  corral start demo/dev-backend --unique --cwd <worktree> --prompt "先读 AGENTS.md，再读 <任务文件>，照做。命令都在前台跑完，全部做完后，回复最后一行写 DONE。" -- codex --yolo -m gpt-6-astra -c 'model_reasoning_effort="high"'
  ```

- 被委派的 agent 一律免确认启动，中途不弹权限框，也不会被自动检查拦下。agent 命令按下表写，后面接上第 3 节的分档参数：

  | agent | agent 命令 |
  |---|---|
  | Claude Code | `claude --dangerously-skip-permissions` |
  | Codex | `codex --yolo` |
  | pi | `pi`（本来就没有权限框） |
  | omp | `omp --approval-mode yolo` |

  免确认也就没有安全网：能约束它的只剩任务文件里「不要做」那几条，所以那一节要写全。免确认跳不过「是否信任这个目录」的框，见下一条。
- 开完运行 `corral wait <名字> --timeout 60`，不要用 sleep 等。60 秒到了状态还是 `starting`，就是卡在「是否信任这个目录」这类对话框上（新项目、新仓库第一次开时常见）。这时请用户在另一个终端窗口运行 `corral attach <名字>`，或者在看板里选中它去点。不要替它点，也不要让用户在你这里用 `!` 运行。卡在启动中时，提醒不会来，不要干等。
- 写代码都是长任务：按 corral 技能「长任务」一节挂提醒，交出去就结束这一轮。
- 告诉用户：开了谁、哪家哪一档、在哪个 worktree、做什么、做完怎么通知。
- 开出来的 agent 默认不关。写代码的 agent 留到它的任务合并以后。

## 5. 主控审查：不直接信「做完了」

每件都审，但要轻，重点是核实，不是逐行重看：

- 读完成记录和回复。关键代码抽看（入口、改动最大的、最容易错的地方）。
- 自己重跑测试，核对数字和对方说的一致。
- 能实测的挑一两条实测：调接口、用临时数据跑一遍、看截图、验算样例。
- 核对范围：只动了该动的目录，「不要做」里的一件都没做。
- 对方说「用户同意了」「用户说过」的，找用户核实。
- 对方列的取舍逐条表态：同意的记下，不同意的交回去改。
- 结论写三五行，追加到任务文件末尾「## 主控审查」。

有问题就写清楚，交回原来的 agent 改：用 `corral send` 交过去，同样带 DONE 约定、挂提醒。改完再审。

## 6. 交叉审查：只给出错代价大的

要不要交叉审查，照第 3 节路由的结论。路由拿不准或不可用时自己定：只给数据模型、并发和事务、安全和隐私、核心规则这类；改文字、查资料、小修不做。

- 另开一个 agent 只读审查，用「重」档。审查一律给 Codex：它更会挑细节。被审的就是 Codex 写的时候，另开一个新的 Codex 实例，只给它任务文件和 diff，不用原来那个。审查任务也写成文件，依次写：
  - 背景：谁在哪个分支做了什么，主控已经审过什么；
  - 先读；
  - 要审查的代码：worktree、`git diff main...<分支>`，**只读，不改、不提交、不切分支**；写明可以跑哪些测试、不能碰什么；
  - 重点看：主控列 5 到 7 条；
  - 输出：追加到审查文件「## 审查意见」。先写一句结论（可以合并 / 改完再合并），每条意见写级别（必须改 / 建议改 / 可以不改）、位置、问题、改法，最后对取舍逐条表态；
  - 回复：只写结论和条数，最后一行 DONE。
- 主控逐条判断。只有「必须改」挡合并；「建议改」可以不采纳，写明理由。认可的交给开发 agent 改。
- 改完请同一个审查者复核，只看改动部分。**复核两轮还没到「可以合并」，停下来问用户**，不再往返。
- 主控和审查者有分歧的，列给用户。

## 7. 合并和收尾

- 合并前：测试全过、主控审查过、要交叉审查的已经是「可以合并」。任务说了不合并的，停在审查，汇报给用户。
- 按 AGENTS.md 的方式合并，合并提交里写一句摘要；推不推送按 AGENTS.md，没写就问。
- 合并后：审查结论追加到任务文件末尾；有集成方在等的，现在通知它。
- 关 agent、删 worktree 之前，按顺序核对，并在回复里说明结果：
  1. `git -C <worktree> status --short` 没有未提交的改动；
  2. 分支已合进 main：`git merge-base --is-ancestor <分支> main`；
  3. `git worktree remove` 不加 `--force`，分支用 `git branch -d` 删。
- 关不关由用户定。项目有交接文件的，更新它。

## 8. 什么自己定，什么先问

- 小事主控先定：写进文档，事后告诉用户。
- 先问用户再做：涉及隐私；花钱（订阅额度、付费服务、真实调用）；改用户批准过的设计；删除东西；推送；关掉 agent。
- 问的方式：一次最多五个问题，按重要程度排，每个问题给选项和你的建议。
