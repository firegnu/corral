# corral 工作规则

## 先读

- `docs/DESIGN.md`：设计，所有决定的来源。改设计先改这里。
- `docs/SPIKE.md`：试验清单和结果。当前阶段是试验。

## 当前阶段：试验

- 试验代码只放 `spike/`，允许粗糙；正式版按试验结论另起。
- 每做完一条检查，立刻把结论和证据写进 `docs/SPIKE.md` 的结果表。
- 发现设计要改的地方，先在 `docs/SPIKE.md` 记下，再和人确认后改 `docs/DESIGN.md`。
- 试验用的工作目录、状态目录放短路径下（macOS 上 Unix socket 路径不能超过 104 字节），用完清理。
- 送给真实 agent 的话尽量短，能用便宜模型就用便宜模型。

## 硬规则

- **只用 Python 标准库**，不装任何第三方包，不依赖 tmux、herdr 或其他终端工具。
- **corral 不认识任何具体的上层工具**：代码、文档、测试、示例里不出现任何具体流程工具的名字或概念；示例名字用 `demo/alice` 这类中性写法。
- **只提供机制**：不在 corral 里加「什么时候开谁、送什么话、空闲后做什么」这类判断。
- **不修改任何全局配置文件**（`~/.claude/settings.json`、`~/.codex/config.toml`、`~/.codex/hooks.json` 等）。钩子只能随 corral 启动的那个 agent 生效。
- **不自动替 agent 回答对话框**，除非是试验里明确要测的那一步。
- **只操作自己用 corral 启动的 agent**，不碰本机其他正在跑的 agent 或终端会话。
- 钩子命令不往标准输出写任何东西。
